generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Destination {
  id          String   @id @default(cuid())
  name        String
  day         Int
  location    String
  description String   @db.Text
  image       String
  price       String
  createdAt   DateTime @default(now())
  // diubah agar database punya nilai cadangan saat di-insert lewat Prisma Studio
  updatedAt   DateTime @default(now()) @updatedAt
}